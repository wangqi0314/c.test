#include <iostream>

using namespace std;
/**
 * 变量定义和初始化
 * type variable_list;
 * type variable_name = value;
 */
int main ()
{
    int d = 3, f = 5;           // 定义并初始化 d 和 f
    char x = 'x';               // 变量 x 的值为 'x'

    cout << d + f << endl;
    cout << x << endl;

    return 0;
}